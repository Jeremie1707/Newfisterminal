// here we are calling the fonction replaceElement defined in app/javascript/components
// In the first parameter we indicate the html id where  the element to replace is
// the second parameter is the partial that we input
replaceElement('#loadin-list', "<%= j render 'dashboard/components/load_ins_rows' %>")
replaceElement('#loadin-nav', "<%= j render 'dashboard/components/nav_pagination' %>")


// $('body').on('hidden.bs.modal', '.modal', function () {
//       $(this).removeData('bs.modal');
//     });
