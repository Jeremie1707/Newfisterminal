if (document.getElementById('my-modal') != null) {
  document.getElementById('my-modal').remove();
  document.querySelector('body').insertAdjacentHTML('beforeend','<%= j render partial: "dashboard/IN/components/detail_load_in" %>');
}
else {
  document.querySelector('body').insertAdjacentHTML('beforeend','<%= j render partial: "dashboard/IN/components/detail_load_in" %>');
}

showModal();
