if (document.querySelector('.in-assignment-form-edit') != null) {
  document.querySelector('.in-assignment-form-edit').remove();
  console.log("remove");
  document.querySelector('.modal-body >table').insertAdjacentHTML('beforebegin','<%= j render partial: "dashboard/components/in_assignment_modal/in_assignment_modal_edit" %>');
}

else {
  document.querySelector('.modal-body >table').insertAdjacentHTML('beforebegin','<%= j render partial: "dashboard/components/in_assignment_modal/in_assignment_modal_edit" %>');
};

inAssignmentClickCloseEdit();
